import React from 'react';
import s from './Footer.module.scss';

function Footer() {
  return (
    <footer className={`${s.footer} `}>
      <div className="container">Chto tam est jelanie buhnut</div>
    </footer>
  );
}

export default Footer;
